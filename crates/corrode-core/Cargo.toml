[package]
name = "corrode-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Shared protocol types between the Corrode daemon, web server, and wasm webui."

[dependencies]
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true
